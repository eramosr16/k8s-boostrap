# Copy this file to `.env` and fill in the real secrets before running `./scripts/run-all.sh`.
# Do not commit your real `.env` file—its values are sensitive.

POSTGRES_PASSWORD=
REDIS_PASSWORD=
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=
KEYCLOAK_ADMIN_PASSWORD=
LETS_ENCRYPT_EMAIL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ACCOUNT_ID=
AWS_REGION=us-east-1
