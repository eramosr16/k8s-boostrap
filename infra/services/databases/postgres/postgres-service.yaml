apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infra
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
      nodePort: 30432
  selector:
    app: postgres
