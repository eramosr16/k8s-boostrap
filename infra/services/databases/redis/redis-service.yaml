apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: infra
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
      nodePort: 30379
  selector:
    app: redis
