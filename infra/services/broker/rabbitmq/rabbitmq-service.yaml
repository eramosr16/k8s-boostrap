apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: infra
spec:
  type: ClusterIP
  ports:
    - port: 5672
      targetPort: 5672
      protocol: TCP
      name: amqp
  selector:
    app: rabbitmq