apiVersion: v1
kind: Service
metadata:
  name: seq
  namespace: infra
  labels:
    app.kubernetes.io/name: seq
    app.kubernetes.io/part-of: seq
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: seq
  ports:
    - name: http
      port: 5341
      targetPort: 5341
      protocol: TCP